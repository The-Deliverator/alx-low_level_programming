#include "main.h"

/**
 * _sqrt - calculates square root
 * @n: numbedr to calculate square root
 *
 * Return: square root
 */
int _sqrt_recursion(int n);
     {
	int sqrt = i * i

		if (sqrt > n)
			return (-1);
	if (sqrt == n)
		return (i);

	return (_sqrt(n, i + 1));
}
